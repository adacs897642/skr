Сервер
устанавливаем Flask (сам фреймворк), python-dotenv (читает настройки из файла)
pip install Flask
pip install python-dotenv
pip freeze - просмотр установленных пакетов
pip install psycopg2
pip install -U Flask-SQLAlchemy
